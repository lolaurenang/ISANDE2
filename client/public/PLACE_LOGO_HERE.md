# Logo

Save the shop's existing logo here as **`logo.png`** (the orange handlebar mark
with "Andoy's Enterprises", already used on the Facebook page).

`client/src/components/Logo.jsx` picks it up automatically. Until you add it,
the app renders a plain wordmark instead — nothing breaks either way.

Suggested export: PNG with a transparent background, around 520 px wide.
